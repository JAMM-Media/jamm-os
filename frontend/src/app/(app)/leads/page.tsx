// path: frontend/src/app/(app)/leads/page.tsx
// The table lives in layout.tsx. This page renders nothing -- the layout
// shows only the full-width table when no lead is selected.
export default function LeadsNoSelection() {
  return null
}
